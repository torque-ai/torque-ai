const { ACTIVITY_ROUTES } = require('./activities');
const { FACTORY_V2_ROUTES } = require('./factory-routes');
const { PII_SCAN_ROUTE } = require('./special-routes');

module.exports = {
  ACTIVITY_ROUTES,
  FACTORY_V2_ROUTES,
  PII_SCAN_ROUTE,
};
