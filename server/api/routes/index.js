const { FACTORY_V2_ROUTES } = require('./factory-routes');
const { PII_SCAN_ROUTE } = require('./special-routes');
const { ASSET_ROUTES } = require('./assets');

module.exports = {
  ASSET_ROUTES,
  FACTORY_V2_ROUTES,
  PII_SCAN_ROUTE,
};
