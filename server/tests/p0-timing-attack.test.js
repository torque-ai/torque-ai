// Auth middleware (auth/middleware.js) has been removed.
// These timing-attack tests depended entirely on it and are no longer applicable.
// The file is kept as a placeholder in case auth timing tests are re-added later.

describe('API key timing-safe authentication', () => {
  it.skip('auth/middleware removed — timing-attack tests no longer applicable', () => {});
});
