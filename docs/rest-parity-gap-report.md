# REST-parity gap report

Generated by `scripts/rest-parity-audit.js` on 2026-05-08T15:38:05.539Z.

## Summary

| Metric | Count |
|---|---|
| MCP tools defined (across `server/tool-defs/` + plugin `tool-defs.js`) | 751 |
| MCP tools with a REST mapping (`tool: 'name'` in any route file) | 750 |
| **MCP tools missing REST coverage (gap)** | **1** |
| Distinct `tool:` references in route files | 751 |
| REST routes pointing at non-existent tools (drift) | 1 |

Coverage: **99.9%**

## Gaps by category

| Category | Tools without REST | First few |
|---|---|---|
| core: protocol | 1 | `ack_notification` |

## Full gap list

### core: protocol (1)

- `ack_notification` — defined in `server\transports\sse\protocol.js`

## Orphaned REST routes

REST routes that proxy to a `tool:` name not found in any tool-defs file. Either the tool was renamed/removed (route should follow), or the tool lives outside the standard tool-defs directories (in which case the audit script should be extended).

- `set_project_config` — referenced by `server/api/routes.js`

## How to use this report

- Treat each category as a phase. Phases with the most operational impact (lifecycle, scheduling, host management) come first.
- Each new REST route belongs in `server/api/routes.js` (or the relevant `v2-*` handler file) using the existing declarative shape: `{ method, path, tool, mapBody | mapQuery | mapParams }`.
- Re-run this script after each phase to track progress.
