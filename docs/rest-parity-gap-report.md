# REST-parity gap report

Generated by `scripts/rest-parity-audit.js` on 2026-05-09T14:18:25.953Z.

## Summary

| Metric | Count |
|---|---|
| MCP tools defined (across `server/tool-defs/` + plugin `tool-defs.js`) | 751 |
| REST-applicable MCP tools | 750 |
| MCP tools with a REST mapping (`tool: 'name'` in any route file) | 750 |
| MCP tools intentionally left MCP-only | 1 |
| **MCP tools missing REST coverage (actionable gap)** | **0** |
| Distinct `tool:` references in route files | 750 |
| REST routes pointing at non-existent tools (drift) | 0 |

Coverage: **100.0%** of REST-applicable MCP tools

## Gaps by category

| Category | Tools without REST | First few |
|---|---|---|

## Intentional MCP-only tools

These tools are excluded from actionable REST-gap totals because their semantics require live MCP/SSE session state.

- `ack_notification` — session-scoped: acks an event delivered via the same SSE stream; defined in `server\transports\sse\protocol.js`

## Full gap list

No actionable REST coverage gaps remain.

## How to use this report

- Treat each category as a phase. Phases with the most operational impact (lifecycle, scheduling, host management) come first.
- Each new REST route belongs in `server/api/routes.js` (or the relevant `v2-*` handler file) using the existing declarative shape: `{ method, path, tool, mapBody | mapQuery | mapParams }`.
- Re-run this script after each phase to track progress.
